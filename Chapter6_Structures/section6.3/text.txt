#include <stdio.h>
#define SIZE 5
#define KEYWORDS int float char double struct union if else while for do switch case break continue return

/* Outer comment with int float char double if else while for do switch case break continue return
   /* Nested comment inside with int float char double if else while for do switch case break continue return */
   End outer comment
*/

/* Multi-line tricky comment
   /* Another nested comment */
   End of multi-line
*/

int main() {
    int i, j, total = 0;
    float avg = 0.0;
    double dval = 2.0;
    char ch1 = 'x', ch2 = '\'', ch3 = '\\';

    struct coords {
        int x;
        int y;
        float z;
    } p1, p2;

    union values {
        int i;
        float f;
        char c;
        double d;
    } u;

    for (i = 0; i < SIZE; i++) {
        for (j = 0; j < SIZE; j++) {
            total += i + j;
            if (total % 2 == 0) {
                continue;
            } else {
                break;
            }
        }
    }

    do {
        total--;
    } while (total > 0);

    switch (total) {
        case 0: break;
        default: continue;
    }

    char *str1 = "int float char double if else while for do switch case break continue return \"nested \\\"quotes\\\"\"";
    char *str2 = "/* not a comment */ int float char double if else while for do switch case break continue return";
    char *str3 = "escaped \\\" int float char double if else while for do switch case break continue return";

    return 0;
}

